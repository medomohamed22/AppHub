import { allowMethods, db, handleError, json, requireUser } from './_lib.js';
const allowed=new Map([['image/png','png'],['image/jpeg','jpg'],['image/webp','webp']]);
export default async function handler(req,res){if(!allowMethods(req,res,['POST']))return;try{const user=await requireUser(req);const {dataUrl,kind='image'}=req.body||{};const match=String(dataUrl||'').match(/^data:(image\/(?:png|jpeg|webp));base64,(.+)$/);if(!match||!allowed.has(match[1]))return json(res,400,{error:'Use PNG, JPG, or WEBP images'});const buffer=Buffer.from(match[2],'base64');if(buffer.length>5*1024*1024)return json(res,413,{error:'Each image must be 5 MB or smaller'});const ext=allowed.get(match[1]);const path=`${user.id}/${Date.now()}-${crypto.randomUUID()}-${kind}.${ext}`;const supabase=db();const {error}=await supabase.storage.from('app-media').upload(path,buffer,{contentType:match[1],upsert:false});if(error)throw error;const {data}=supabase.storage.from('app-media').getPublicUrl(path);return json(res,201,{url:data.publicUrl,path})}catch(e){return handleError(e,res,'Unable to upload image')}}
