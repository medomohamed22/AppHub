import { allowMethods, db, handleError, json, requireUser } from './_lib.js';
export default async function handler(req,res){if(!allowMethods(req,res,['POST']))return;try{const user=await requireUser(req);const {paymentId,purpose,amountPi,appId}=req.body||{};if(!paymentId||!purpose||!Number(amountPi))return json(res,400,{error:'Missing payment data'});const key=process.env.PI_SECRET_KEY;if(!key)throw new Error('PI_SECRET_KEY is not configured');const response=await fetch(`https://api.minepi.com/v2/payments/${paymentId}/approve`,{method:'POST',headers:{Authorization:`Key ${key}`,'Content-Type':'application/json'}});const data=await response.json().catch(()=>null);if(!response.ok)return json(res,response.status,{error:data||'Pi approve request failed'});const {error}=await db().from('payments').upsert({user_id:user.id,app_id:appId||null,payment_id:paymentId,purpose,amount_pi:Number(amountPi),status:'approved',raw_response:data},{onConflict:'payment_id'});if(error)throw error;return json(res,200,{approved:true,data})}catch(e){return handleError(e,res,'Unable to approve payment')}}
