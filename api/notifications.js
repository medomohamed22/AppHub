import { auth,body,db,handler,json,method,rateLimit } from '../lib/core.js';
export default async function(req){return handler(async()=>{const bad=method(req,['GET','POST']);if(bad)return bad;const c=db(),u=await auth(req);await rateLimit(c,`notify:${u.id}`,40,60);if(req.method==='GET'){const {data,error}=await c.from('notifications').select('*').eq('user_id',u.id).order('created_at',{ascending:false}).limit(50);if(error)throw error;return json({notifications:data});}const x=await body(req);let q=c.from('notifications').update({read_at:new Date().toISOString()}).eq('user_id',u.id).is('read_at',null);if(x.id)q=q.eq('id',x.id);const {error}=await q;if(error)throw error;return json({ok:true});},req)}
