import fs from 'node:fs';import path from 'node:path';import {execFileSync} from 'node:child_process';const root=new URL('..',import.meta.url).pathname;const api=fs.readdirSync(path.join(root,'api')).filter(x=>x.endsWith('.js'));if(api.length!==12)throw new Error(`Expected 12 API files, found ${api.length}`);for(const d of ['api','lib','scripts'])for(const f of fs.readdirSync(path.join(root,d)).filter(x=>x.endsWith('.js')||x.endsWith('.mjs')))execFileSync(process.execPath,['--check',path.join(root,d,f)],{stdio:'inherit'});const html=fs.readFileSync(path.join(root,'index.html'),'utf8');if(/SUPABASE_SERVICE_ROLE|PI_API_KEY|APP_JWT_SECRET/.test(html))throw new Error('Secret name leaked into index');console.log('OK: 12 API files, JavaScript syntax valid, no server secret identifiers in frontend.');
